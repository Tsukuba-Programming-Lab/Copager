[package]
name = "copager_parse_common"
edition = "2024"
version.workspace = true

[dependencies]
copager_lang = { path = "../lang" }

[dev-dependencies]
copager_lang = { path = "../lang", features = ["derive"] }
